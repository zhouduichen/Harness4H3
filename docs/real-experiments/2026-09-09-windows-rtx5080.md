# Real H3 execution record — 2026-09-09

This is an evidence record, not a claim that M5 has passed.

## Controller and Inspector

The Windows host `autoresearch-5080` was reachable over Tailscale SSH as the configured user. Ollama exposed `qwen3.5:9b-q8_0` (9.7B, Q8_0) on `100.88.143.10:11434`. ComfyUI was started with the existing H3 launcher arguments plus `--listen 127.0.0.1,100.88.143.10 --reserve-vram 1`; `/system_stats` reported RTX 5080, 33.75GB RAM and 16.3GB VRAM.

The real NVFP4 checkpoint header was parsed remotely without loading weights:

| field | value |
| --- | ---: |
| file bytes | 12,528,636,800 |
| tensor count | 1,132 |
| parameter elements in tensor headers | 11,681,874,744 |
| blocks | 50 |
| qkv shape | `[21504, 2688]` |
| H3 signature | complete (7/7 tensors) |
| storage dtypes | U8, F8_E4M3, BF16, F16, F32 |

The mixed storage dtypes are represented as `ModelState.dtype="mixed"`; quantization is separately recorded as `nvfp4`.

## M4 real H3-derived child

On the same Windows host, the retained INT8 ConvRot H3 checkpoint was inspected as `M0000` (20,970,379,616 bytes, 50 blocks, complete H3 signature). `PrebuiltQuantizeOperator(bits=4)` selected the fixed NVFP4 deployment artifact and produced `M0001` (12,528,636,800 bytes, 50 blocks, complete H3 signature), a 40.27% model-size reduction. The parent file was not modified. Because the operator only adopts a trusted prebuilt variant, the child is explicitly marked `metrics_stale=true` until held-out quality and hardware measurements are attached.

## M2 real LLM + Fake H3

Using the Windows Qwen3.5 controller against the offline Fake H3 registry:

```text
M0000 → quantize(bits=4) → M0001 → step_distill(target_steps=22) → M0002
```

The run reached `target_satisfied` in 2 experiments, 0 failed experiments, 0.01 GPU-hours of fake cost and 0.5 seconds of fake operator wall time. This validates the structured LLM/controller boundary, not real H3 optimization.

## Real ComfyUI probes

All runs used the same sanity prompt and seed 42, and the independent video evaluator.

| recipe | result | score | wall time |
| --- | --- | ---: | ---: |
| NVFP4, 20 steps | 22 frames, 352×640, decodable, no black frames | 0.991137 | 223.23s |
| NVFP4, 4 steps | 22 frames, 352×640, decodable, no black frames | 0.931521 | 43.77s |
| NVFP4, 8 steps | black-frame gate | 0.49 | 30.47s |
| NVFP4, 6 steps | decodable, no black frames | 0.861886 | 24.45s |
| Q4_K GGUF, 4 steps | black-frame gate | 0.49 | 36.73s |

The 4-step run is faster but its score drop from the 20-step reference is about 5.99%, above the configured 5% quality-drop limit. The 8-step and Q4_K runs fail the black-frame gate. Therefore no M5 quality-preserving measurable improvement is promoted yet; the artifacts remain under the local ignored `var/` directory for replay.
