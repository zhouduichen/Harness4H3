name: CPU CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  tests:
    runs-on: ubuntu-latest
    timeout-minutes: 20
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      - name: Install test and training dependencies
        run: python -m pip install --upgrade pip && python -m pip install -e ".[test,training]"
      - name: Run CPU test suite
        run: python -m pytest -q
      - name: Compile Python sources
        run: python -m compileall -q harness4h3 h3_training research tools
