[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "harness4h3"
version = "0.2.0"
description = "EvoGen-RSI Phase-I model optimization harness for MiniMax H3"
readme = "README.md"
requires-python = ">=3.9"
dependencies = [
  "PyYAML>=6.0,<7",
  "opencv-python-headless>=4.8,<5",
]

[project.optional-dependencies]
test = ["pytest>=8,<9"]

[project.scripts]
harness4h3 = "harness4h3.cli:main"

[tool.setuptools.packages.find]
include = ["harness4h3*"]

[tool.pytest.ini_options]
testpaths = ["tests"]
addopts = "-ra"
